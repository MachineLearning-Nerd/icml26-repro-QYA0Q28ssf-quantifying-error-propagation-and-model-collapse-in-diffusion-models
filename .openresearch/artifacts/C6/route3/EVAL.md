# C6 route 3 — CIFAR-10 source contract

Pre-run status: **formal cumulative run pending**.

The exact inherited command is:

```bash
uv run --frozen python -m repro_campaign.run
```

This route can establish only an evaluator-visible BLOCKED reason. Missing settings
or missing direct evidence are never converted to verification or falsification.
