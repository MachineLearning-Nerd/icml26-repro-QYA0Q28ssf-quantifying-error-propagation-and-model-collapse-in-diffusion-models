# C6 route 1 — full-scale population-KDE GMM

Pre-run status: **formal OpenResearch run pending**.

Exact inherited command:

```bash
uv run --frozen python -m repro_campaign.run
```

The route must expose all 10 seed rows, 95% intervals, the full 21-generation
population curve, CPU/runtime metadata, and the `h=0` control. Its final C6 status
remains `BLOCKED` regardless of alignment because two material substitutions and
both image datasets remain.
