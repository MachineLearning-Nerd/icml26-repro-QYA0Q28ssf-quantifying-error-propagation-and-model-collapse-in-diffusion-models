# C6 route 2 limitations and deviations

- No image model is trained and no empirical claim is accepted.
- The microbenchmark is intentionally much smaller than the described U-Net.
- The runtime projection excludes observability regressors, feature classifiers,
  evaluation, checkpoint I/O, and early-stopping uncertainty.
- Inventing the missing settings would prevent an exact claim-level reproduction.
