# C6 route 2 — Fashion-MNIST feasibility

Pre-run status: **formal HF CPU benchmark pending**.

The exact inherited command is:

```bash
uv run --frozen python -m repro_campaign.run
```

This route can only establish a concrete BLOCKED reason. It cannot earn verification
credit for C6.
