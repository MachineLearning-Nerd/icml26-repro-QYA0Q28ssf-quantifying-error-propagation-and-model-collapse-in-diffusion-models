# C4–C5 method

C4 is reconstructed from the exact refresh identity, innovation lower recursion,
the clean C2 lower bound, finite summation, and a Cesàro-to-limsup argument. A
sequence with `eps_i^2=1/(i+1)` checks that series divergence alone permits
`D_i->0`; a constant-floor sequence attains the proposition's factor
`alpha/(16*(1+(1-alpha)^2))`.

C5 is audited as a quantified mathematical statement. A geometric summable sequence
is evaluated at increasing horizons for `alpha=0.1,0.5,0.9`. The calculation reports
the minimum upper `asymp` constant forced by `D_N+C_bias>=C_bias`; it diverges as the
right side vanishes. This numerical sequence illustrates a proof-level limit
contradiction and is not itself substituted for the proof.

Two falsification mutations are required to remove the contradiction: set
`C_bias=0`, or add the same bias to the right side. The independent checker rejects
the FALSIFIED certificate under either mutation.
