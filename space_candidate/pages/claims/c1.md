# C1 — Proposition 3.1: VERIFIED

## Exact contract

Source hash `472fb9e246aea7c5d1e643d5755f034f005d1f2bbb61f86371bae855f62765e3`,
anchors A1 `#S3.I1.i1`, A2 `#S3.I1.i2`, Proposition 3.1
`#S3.Thmtheorem1`. For every eligible generation, finite learned-path drift energy
and a true Girsanov martingale imply

`KL(p_hat^(i+1) || q_i) <= KL(P_hat_i || P_star_i) = eps_hat_i²/2`.

The universal quantifier is carried by an independently checked derivation:
learned-measure Girsanov log density, zero-mean Itô integral, path KL identity, and
endpoint data processing. [Contract](/evidence/artifacts/C1/claim_contract.json),
[source audit](/evidence/artifacts/C1/source_audit.md), and
[certificate](/evidence/artifacts/C1/proof_certificate.json).

## Direct evidence

HF run `3621fc83-117d-44b8-9b88-c04406773d68`, Git
`20bf5fed2d69b8b16017516b6bfe70b03b9bf816`, five seeds
`57721, 61169, 65537, 70001, 74959`, 20,000 trajectories per beta:

| beta | eps_hat² | exact path KL | logistic endpoint KL | kNN endpoint KL |
| ---: | ---: | ---: | ---: | ---: |
| 0.1 | 0.280516 | 0.140258 | 0.040571 | 0.175491 |
| 0.2 | 1.093876 | 0.546938 | 0.168239 | 0.496945 |
| 0.3 | 2.394588 | 1.197294 | 0.370489 | 0.940197 |

Neither endpoint estimator significantly contradicts the exact path budget. This
calibration uses the paper's 10D five-component geometry, 500 reverse steps,
`T=4`, and `t0=0.02`. **Assumption audit:** A1 and a Novikov sufficient condition
for A2 both pass.

## Checker, controls, and reproduction

[Verifier](/evidence/code/repro_campaign/claim1_girsanov.py) and
[independent checker](/evidence/code/repro_campaign/check_claim1.py). The checker
rejects a mutated `0.49` coefficient. Both endpoint estimators reject a deliberately
false 0.1× path budget. The earlier wrong-direction confidence gate is retained as
[Historical rejected route](/evidence/artifacts/C1/rejected_estimator_route.json).
[Raw JSON](/evidence/artifacts/C1/raw_formal.json).

```bash
uv run --frozen python -m repro_campaign.run
```

This is the **fixed command** for every experiment node.
Estimated 8 CPU workers; HF `cpu-upgrade` advertised 8 vCPU. The container reported
64 logical/affinity CPUs; active parallel work was bounded to 8. Verifier runtime
30.758 s; job duration 53 s. Pinned Python 3.12 environment is linked from the
[index](#/index).

**Limitation:** the calibration drift is controlled, not a trained KDE score. It is
corroboration; the exact certificate carries the universal statement.
