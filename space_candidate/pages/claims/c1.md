# C1 — Proposition 3.1

Current pre-publication status: **exact certificate accepted; fresh-seed formal
confirmation pending**.

Under A1 (finite learned-path drift energy) and A2 (valid Girsanov martingale),
the exact source claim is

`KL(p_hat^{i+1} || q_i) <= KL(P_hat_i || P_star_i) = eps_hat_i^2 / 2`.

The current verifier reconstructs the learned-measure Girsanov log density, checks the
zero-mean Itô term, derives the path KL identity, and applies endpoint data processing.
Its negative control changes `1/2` to `0.49`; the independent checker rejects that
certificate with `wrong coefficient` and `exact conclusion mismatch`.

The calibration uses the paper's 10D, five-component GMM geometry, 500 reverse-SDE
steps, `T=4`, `t0=0.02`, five fresh confirmation seeds, and a bounded state-dependent error
`beta*tanh(x)`. A1 and Novikov's sufficient condition for A2 are finite for all three
beta values. Neither endpoint estimator may significantly contradict the exact
discrete path-KL budget, and both must reject a deliberately false 0.1× budget.

The first numerical route is preserved as **Historical rejected route**: it required
each noisy estimator's upper confidence value to fall below the bound, which is not
a valid test for a significant violation. Raw confirmation results and CPU/runtime
fields will replace this pending note only after the OpenResearch run exits zero.

Limit: the controlled error is not a trained KDE score. The proof certificate—not the
finite calibration—carries the universal quantifier.
