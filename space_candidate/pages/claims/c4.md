# C4 — Proposition 4.1: VERIFIED

## Exact contract and corrected quantifiers

Source anchor `#S4.Thmtheorem1` has two different clauses:

1. `sum eps_i²=infinity` implies `sum D_i=infinity`.
2. The displayed nonzero limsup floor additionally assumes an eventual pointwise
   `eps_i²>=eps_floor>0`, yielding
   `limsup D_i >= alpha*eta_floor*eps_floor /
   (16*(1+(1-alpha)²))`.

The imported judge wording merged them. Divergence of a positive series alone does
not imply its terms have a floor. [Source audit](/evidence/artifacts/C4-C5/source_audit.md),
[contract](/evidence/artifacts/C4-C5/claim_contract.json), and
[certificate](/evidence/artifacts/C4-C5/proof_certificate.json).

## Direct evidence

The checker reconstructs exact chi-squared refresh contraction, innovation lower
bound, the clean C2 intra-generation bound, summed recurrence, Cesàro floor, and
limsup step.

| alpha | eta floor | eps² floor | exact paper limsup floor |
| ---: | ---: | ---: | ---: |
| 0.1 | 0.5 | 0.01 | `1.72652e-5` |
| 0.5 | 0.5 | 0.01 | `1.25000e-4` |
| 0.9 | 0.5 | 0.01 | `2.78465e-4` |

The independent control `eps_i²=1/(i+1)` constructs recurrence-valid `D_i->0` while
both series diverge, proving why clause 1 alone gives no pointwise floor.
**Assumption audit:** the checker separately requires the source's series condition
and the stronger eventual pointwise floor before accepting the displayed bound.

## Checker, controls, and reproduction

HF run `1a6f4c55-710c-44cb-b108-e281f7f6ce80`, Git
`a6ace14d8ab3a444861e439e687da44af80586fd`.
[Verifier](/evidence/code/repro_campaign/claim45_global.py),
[checker](/evidence/code/repro_campaign/check_claim45.py), and
[raw JSON](/evidence/artifacts/C4-C5/raw_formal_summary.json). Removing the uniform
error floor makes the checker reject the floor conclusion.

```bash
uv run --frozen python -m repro_campaign.run
```

This is the **fixed command** for every experiment node.
Estimated/active CPU workers 8; HF `cpu-upgrade` advertised 8 vCPU, container
reported 64 logical/affinity CPUs. Verifier runtime 33.247 s; job runtime 58 s.

**Limitation:** C4 is verified with the source's two clauses separated; no claim is
made that series divergence alone forces a nonzero termwise floor.
