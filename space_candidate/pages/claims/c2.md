# C2 — Proposition 3.3

Current pre-publication status: **exact certificate and non-Gaussian calibration
pending a formal run**.

The exact source statement assumes A1–A4 and `eps_star^2<=1`, defines
`eta=Var(E[M|Y])/eps_star^2`, and concludes

`chi2 >= eta*eps_star^2/4 - C*eps_star^4`.

The current verifier checks the complete conditional-Girsanov, Taylor, Young,
observability, and remainder-control derivation, including the clean `1/8` regime.
It rejects a certificate whose leading `1/4` coefficient is changed to `0.24`.

The calibration uses an exact continuous diffusion whose time-dependent drift has
separately tunable observable and endpoint-orthogonal components. It spans `eta=0`
through `eta=1` analytically and repeats the calculation on a non-Gaussian symmetric
mixture endpoint. Formal raw rows will replace this note only after a zero-exit run.
