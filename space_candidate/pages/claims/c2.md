# C2 — Proposition 3.3: VERIFIED

## Exact contract

Definition 3.2 defines
`eta=Var(E[M_T|Y_t0])/eps_star²` in `[0,1]`. Under A1–A4 and
`eps_star²<=1`, for every eligible generation,

`chi²(p_hat^(i+1)||q_i) >= eta*eps_star²/4 - C*eps_star⁴`.

The constant depends on the source A3–A4 moments. Anchors:
`#S3.Thmtheorem2`, `#S3.I2.i3`, `#S3.I2.i4`, and
`#S3.Thmtheorem3`. [Source audit](/evidence/artifacts/C2-C3/source_audit.md),
[contract](/evidence/artifacts/C2-C3/claim_contract.json), and
[certificate](/evidence/artifacts/C2-C3/proof_certificate.json).

## Direct evidence

The certificate reconstructs conditional Girsanov projection, exponential Taylor
remainder, weighted Young inequalities, observability transfer, and Hölder/BDG
remainder control. It produces the exact leading coefficient `1/4` and clean
coefficient `1/8`.

HF run `29594092-3b31-4ea6-ac75-57b13f571bbe`, Git
`82ebd6a282101409cde92fbf81fb1dedbd08a386`:

| Audit item | Observed |
| --- | --- |
| Endpoint families | Gaussian and symmetric non-Gaussian two-component mixture |
| rho sweep | 0, 0.1, 0.3, 0.6, 1 |
| beta sweep | 0.02, 0.05, 0.1, 0.2 |
| non-Gaussian Fisher information | 0.962640 |
| non-Gaussian eta range | 0 to 0.962640 |
| chi²/(eta eps²), positive eta | 1.00005 to 1.01725 |
| maximum independent quadrature disagreement | `6.94e-18` |
| A1–A4 audits | all pass |

The construction has independently tunable endpoint-visible and time-orthogonal
drift components, so eta is not fixed to one. **Assumption audit:** every explicit
A1–A4 condition in the contract is reconstructed and checked before acceptance.

## Checker, controls, and reproduction

[Verifier](/evidence/code/repro_campaign/claim23_observability.py),
[checker](/evidence/code/repro_campaign/check_claim23.py), and
[raw JSON](/evidence/artifacts/C2-C3/raw_formal_summary.json). A `0.24` leading
coefficient mutation is rejected; a false `1.10*eta*eps²` lower bound is detected.

```bash
uv run --frozen python -m repro_campaign.run
```

This is the **fixed command** for every experiment node.
Estimated 8 CPU workers; HF `cpu-upgrade` advertised 8 vCPU, container reported 64
logical/affinity CPUs, active work bounded to 8. Verifier runtime 33.532 s; job
runtime 59 s.

**Limitation:** the finite families calibrate the proof but do not prove a universal
theorem. The checked symbolic derivation carries the quantifier.
