# C3 — Theorem 3.4: VERIFIED

## Exact contract

Under A1–A4, Theorem 3.4 combines C2 with

`chi² <= 4*eps_star² + c*eps_star⁴`.

The two-sided equivalence additionally requires a uniform
`eta>=eta_floor>0` and sufficiently small error. Eta-zero cases do not count as
equivalence evidence. Source anchor `#S3.Thmtheorem4`;
[full source audit](/evidence/artifacts/C2-C3/source_audit.md).

## Direct evidence

The same exact certificate used for C2 derives the upper coefficient `4` via an
elementary square bound, Jensen/Itô second-moment control, and the A3–A4 remainder.
The calibration spans eta zero to one on Gaussian endpoints and zero to `0.962640`
on a non-Gaussian mixture. For positive eta,
`chi²/(eta*eps²)=1.00005–1.01725`, consistent with two-sided scaling, while eta-zero
rows remain explicit edge controls.

Run `29594092-3b31-4ea6-ac75-57b13f571bbe`, Git
`82ebd6a282101409cde92fbf81fb1dedbd08a386`, verifier 33.532 s, HF job 59 s.
[Certificate](/evidence/artifacts/C2-C3/proof_certificate.json) and
[raw JSON](/evidence/artifacts/C2-C3/raw_formal_summary.json).

## Checker, controls, and reproduction

[Verifier](/evidence/code/repro_campaign/claim23_observability.py) and
[checker](/evidence/code/repro_campaign/check_claim23.py). A coefficient-`3.9`
mutation is rejected, and a false `chi²<=0.50*eps²` numerical control fails.

```bash
uv run --frozen python -m repro_campaign.run
```

This is the **fixed command** for every experiment node.
Estimated/active workers: 8. HF `cpu-upgrade` advertised 8 vCPU; container
logical/affinity count 64. Verifier runtime 33.532 s and job runtime 59 s.
Environment and seeds are identical to C2.

**Limitation:** the certificate is a machine-checked derivation graph, not a kernel
proof-assistant object. This creates evaluator risk despite the exact assumptions,
qualifiers, mutation controls, and independent numerical reconstruction.
