# C6 route 2 — Fashion-MNIST feasibility

Final route status: **BLOCKED**.

The exact inherited command is:

```bash
uv run --frozen python -m repro_campaign.run
```

HF run `887cb735-56a2-405e-b98e-5ec5e9d37245` at commit
`970446a85031a49c66d33be7ee07f33452ac8c9a` found eleven material protocol
fields missing. The same schema accepts a fully populated mock protocol. A
737-parameter three-convolution benchmark yields an optimistic `6.59`-hour lower
bound for the stated CPU workload; it is not an image reproduction and earns no C6
verification credit.
